# CogniLearn — Model venv dependencies
# Install: pip install -r requirements.txt

# Core data / ML
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.4
xgboost>=2.0
shap>=0.45
joblib>=1.4

# Visualisation
matplotlib>=3.8

# Notebook runtime
ipykernel>=6.29
jupyter>=1.0

# RAG pipeline (production path — Azure + Supabase)
openai>=1.30
supabase>=2.4
python-dotenv>=1.0

# EAR fatigue detector (production path — MediaPipe)
mediapipe>=0.10
Pillow>=10.3
